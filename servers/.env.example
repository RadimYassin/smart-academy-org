# Shared Environment Variables for All Microservices
# Copy this file to .env in each service directory and configure as needed

# =============================================================================
# JWT Configuration (REQUIRED - Must be same across all services)
# =============================================================================
# JWT secret key for token signing and verification
# IMPORTANT: Use a strong, random 256-bit secret in production!
# Generate a new secret with: openssl rand -base64 32
JWT_SECRET=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970

# JWT token expiration time in milliseconds
# Default: 86400000 (24 hours / 1 day)
JWT_EXPIRATION=86400000

# JWT refresh token expiration time in milliseconds
# Default: 604800000 (7 days)
JWT_REFRESH_EXPIRATION=604800000

# =============================================================================
# Eureka Configuration (for service discovery)
# =============================================================================
# Eureka server URL
EUREKA_SERVER_URL=http://localhost:8761/eureka

# =============================================================================
# Database Configuration (Service-specific - configure per service)
# =============================================================================
# User-Management Service
# DB_USERNAME=postgres
# DB_PASSWORD=postgres

# Course-Management Service
# DATABASE_HOST=localhost
# DATABASE_PORT=5433
# DATABASE_USER=postgres
# DATABASE_PASSWORD=postgres

# LMS Connector Service (NestJS)
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_USER=postgres
# DATABASE_PASSWORD=postgres
# DATABASE_NAME=lmsconnector

# =============================================================================
# Service Ports
# =============================================================================
# User-Management: 8082
# Course-Management: 8081
# Eureka-Server: 8761
# LMS Connector: 3000

# =============================================================================
# RabbitMQ Configuration (for async messaging)
# =============================================================================
# RabbitMQ credentials (change in production!)
RABBITMQ_USER=admin
RABBITMQ_PASS=admin123

# =============================================================================
# Email Service Configuration (SMTP)
# =============================================================================
# SMTP server settings (example: Gmail)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
FROM_EMAIL=noreply@smartacademy.com

# =============================================================================
# Production Deployment Notes
# =============================================================================
# 1. Generate a strong JWT_SECRET using: openssl rand -base64 32
# 2. Store secrets in a secure vault (e.g., AWS Secrets Manager, Azure Key Vault)
# 3. Use different secrets for different environments (dev, staging, prod)
# 4. Never commit .env files to version control
# 5. Ensure all services use the SAME JWT_SECRET for token validation
