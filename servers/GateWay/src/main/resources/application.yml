server:
  port: 8888

spring:
  application:
    name: gateway-service
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      # Explicit Routes Configuration
      routes:
        # User Management Service Routes
        - id: user-management-service
          uri: http://localhost:8082
          predicates:
            - Path=/user-management-service/**
          filters:
            - StripPrefix=1
        
        # Course Management Service Routes  
        - id: course-service
          uri: http://localhost:8081
          predicates:
            - Path=/course-service/**
          filters:
            - StripPrefix=1
            
        # LMS Connector Service Routes
        - id: lms-connector
          uri: http://lms-connector-service:3000
          predicates:
            - Path=/lmsconnector/**
          filters:
            - StripPrefix=1
            
        # Chatbot-edu Service Routes
        - id: chatbot-edu-service
          uri: http://localhost:8005
          predicates:
            - Path=/chatbot-edu-service/**
          filters:
            - StripPrefix=1
      
      # CORS Configuration
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOriginPatterns: "*"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
              - PATCH
            allowedHeaders: "*"
            exposedHeaders:
              - "*"
            allowCredentials: true
            maxAge: 3600

# JWT Configuration - Must match all microservices
application:
  security:
    jwt:
      secret-key: ${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE:http://localhost:8761/eureka}
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: false
    hostname: gateway-service
