[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --cov=app --cov=services --cov=routers --cov-report=term-missing
env =
    LLM_PROVIDER=mock
    FAISS_INDEX_PATH=mock_index_path
